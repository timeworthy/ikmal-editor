{
  "name": "@ikmal/writing-ui",
  "private": true,
  "type": "module",
  "description": "Portable writing indicator primitives for the ikmal rewrite.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --experimental-strip-types --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
